zookeeper {
  basePath = "/snack/serviceDiscovery"
  connectString = "localhost:2181"
}

server {
  port = 8080
  name = "com.snack.web.demo"
}

rpc {
  invoke.timeout = 5000
  invoke.retry.enable = true
}

# Enable tracing for demo
tracing {
  enabled = true
  sampleRate = 100
}